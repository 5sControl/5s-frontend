server {
    listen 80;

    location = / {
        return 301 /mobile/;
    }

    location = /mobile {
        return 301 /mobile/;
    }

    location /mobile/ {
        alias /usr/share/nginx/html;
        try_files $uri $uri/ /index.html;
    }
}