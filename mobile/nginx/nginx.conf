server {
    listen 80;

    location = / {
        return 301 /mobile/;
    }

    location = /mobile {
        return 301 /mobile/;
    }

    location /mobile/ {
        root /usr/share/nginx/html;
        try_files $uri $uri/ /mobile/index.html;
    }
}