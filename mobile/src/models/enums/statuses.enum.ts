export enum STATUS {
    OK = 200,
    CREATED = 201,
    DELETED = 204
}

export enum OPERATION_STATUS_ENUM {
    PENDING = 'pending',
    IN_PROGRESS = 'in_progress',
    COMPLETED = 'completed',
  }
  